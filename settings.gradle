pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'chat-with-me-be'

// Include the local fast-framework build
includeBuild('../fast-framework')
